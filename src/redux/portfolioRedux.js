// selectors
export const getFullPortfolio = (state) => state.portfolio;

export const portfolioReducer = (statePart = [], action = {}) => {
  switch (action.type) {
    default:
      return statePart;
  }
};
